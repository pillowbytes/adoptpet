<div class="container">

<h1>Ache o seu pet 🐾</h1>
  <% @pets.each do |pet| %></p>
    <img src="<%= pet.image_url%>"alt="Pet" width="500">
    <h2><%= pet.name %></h2>
    <p><%= pet.gender %></p>
  <% end %>
</div>
