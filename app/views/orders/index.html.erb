<div class="container">
  <div class="row">
    <div class="col">
    <h1>⏳Suas ordens🧾</h1>
    </div>
  </div>
  </div>

  <% @orders.each do |order| %>
  <div class="m-5">
  <h2>Ordem #{order.id}</h2>
  <p><%= pet.name %></p>
  <%= link_to "Ver Mais", order_path(order), class: "btn btn-primary"%>
  </div>
  <% end %>
